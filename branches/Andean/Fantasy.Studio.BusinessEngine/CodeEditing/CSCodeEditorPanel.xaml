<UserControl x:Class="Fantasy.Studio.BusinessEngine.CodeEditing.CSCodeEditorPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style TargetType="avalonEdit:TextEditor">
            <Setter Property="SyntaxHighlighting" Value="C#"/>
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="10pt"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="138*" />
            <RowDefinition Height="0" />
        </Grid.RowDefinitions>

        <avalonEdit:TextEditor Name="textEditorTop" Margin="0,0,0,2"/>
        <GridSplitter VerticalAlignment="Bottom"  HorizontalAlignment="Stretch" Width="2"/>
        <avalonEdit:TextEditor Name="textEditorBottom" Grid.Row="1"/>
    </Grid>
</UserControl>
